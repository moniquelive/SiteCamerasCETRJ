:javascript
  (function() {
    function getScript(url,success){
      var script=document.createElement('script');
      script.src=url;
      var head=document.getElementsByTagName('head')[0],
      done=false;
      script.onload=script.onreadystatechange = function(){
        if ( !done && (!this.readyState || this.readyState == 'loaded' || this.readyState == 'complete') ) {
          done=true;
          success();
          script.onload = script.onreadystatechange = null;
          head.removeChild(script);
        }
      };
      head.appendChild(script);
    }

    getScript('//cdnjs.cloudflare.com/ajax/libs/jquery/2.1.1/jquery.min.js',function(){
      getScript('/javascripts/jquery.smooth-scroll.min.js',function(){
        getScript('//cdnjs.cloudflare.com/ajax/libs/angular.js/1.3.0-beta.13/angular.min.js',function(){
          getScript('/javascripts/js.js',function(){ init(); });
        });
      });
    });
  })();
